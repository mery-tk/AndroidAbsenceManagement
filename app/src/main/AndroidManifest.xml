<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ma.ensa.meryem.taikokgestionabsences">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu1"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".NouvelleClasse"/>
        <activity android:name=".SupprimerClasse"/>
        <activity android:name=".MettreAjourClasse"/>
        <activity android:name=".choisirFichierClasse"/>
        <activity android:name=".AfficherAbsences"/>
        <activity android:name=".AfficherEtudiants"/>
        <activity android:name=".ListAbsence"/>
        <activity android:name=".SupprimerAbsence"/>
        <activity android:name=".MettreAjourAbsence"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>